{
  "name": "mathscinet-bibtex-exporter",
  "version": "0.3.0",
  "private": true,
  "description": "A bilingual Chrome extension for reliable, resumable bulk export of MathSciNet search results to BibTeX.",
  "license": "MIT",
  "author": "Yiyang Guo",
  "repository": {
    "type": "git",
    "url": "https://github.com/0neblaze/mathscinet-bibtex-exporter.git"
  },
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "check": "npm test && node --check extension/content.js && node --check extension/i18n.js && node --check extension/lib.js && node --check extension/popup.js && node --check scripts/package-release.mjs",
    "package": "node scripts/package-release.mjs"
  }
}
